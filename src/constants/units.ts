export const BYTES_PER_KB = 1024;

export const BYTES_PER_KBIT = 125;

export const BYTES_PER_MBIT = 125000;

export const KB_PER_MB = 1024;

export const BYTES_PER_MB = BYTES_PER_KB * KB_PER_MB;

export const MB_PER_GB = 1024;

export const NS_PER_SEC = 1e9;

export const US_PER_SEC = 1e6;

export const CLOCK_MS = 1e5;

export const MS_PER_SEC = 1e3;

export const NS_PER_MS = 1e6;

export const NS_PER_US = 1e3;

export const SECONDS_PER_MINUTE = 60;

export const MINUTES_PER_HOUR = 60;

export const HOURS_PER_DAY = 24;

export const SECONDS_PER_HOUR = SECONDS_PER_MINUTE * MINUTES_PER_HOUR;

export const SECONDS_PER_DAY = SECONDS_PER_HOUR * HOURS_PER_DAY;

export const MS_PER_WEEK = 7 * SECONDS_PER_DAY * MS_PER_SEC;

export const MAX_UINT16 = (1 << 16) - 1;

export const MAX_UINT32 = 2 ** 32 - 1;

export const PI_X2 = 2 * Math.PI;
